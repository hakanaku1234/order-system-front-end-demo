import React, {} from 'react';
import LoginForm from '../components/LoginForm';

const App = (...args) => {
  return (
    <div>
      <div>登录</div>
      <LoginForm />
    </div>
  )
};

export default App
